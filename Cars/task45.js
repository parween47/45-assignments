"use strict";
function make_car(manufacturer, model, ...optional) {
    let car = {
        manufacturer,
        model
    };
    for (let i = 0; i < optional.length; i++) {
        mkckndjks.s.kknkn;
        cjn;
    }
}
